# ticket_app/urls/urls.py

# from django.urls import path
# from ticket_app import views

urlpatterns = [
    # Your URL patterns here, e.g.
    # path('events/', views.EventList.as_view(), name='event_list'),
]
